image: openjdk:9

pipelines:
  default:
    - step:
        caches:
          - gradle
          - node
        script:
          - bash ./gradlew build
          - apt install -y zip
          - zip -r template.zip .
